/**
 * Temporary root placeholder for the Task 1 scaffold.
 * Replaced in Task 4/7 by the guarded `(app)` home at `/`.
 */
export default function RootPlaceholder() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2 px-6 text-center">
      <p className="text-sm text-muted-foreground">BiteExpress Agents</p>
      <p className="text-base">Setting things up…</p>
    </main>
  );
}
